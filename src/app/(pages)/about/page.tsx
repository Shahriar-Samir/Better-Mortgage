import Mission from '@/app/(components)/Aboutpage/Mission';
import React from 'react';

const page = () => {
    return (
        <main>
            <Mission/>
            
        </main>
    );
};

export default page;