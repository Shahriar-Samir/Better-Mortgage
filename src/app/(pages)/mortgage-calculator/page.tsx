import Calculator from '@/app/(components)/calculator/Calculator';
import React from 'react';

const page = () => {
    return (
        <main>
            <Calculator/>
        </main>
    );
};

export default page;