import Navbar from '@/app/(components)/startpage/Navbar';
import React from 'react';

const page = () => {
    return (
        <main>
            <Navbar/>
        </main>
    );
};

export default page;